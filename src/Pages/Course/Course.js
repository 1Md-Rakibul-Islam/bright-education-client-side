import React from 'react';

const Course = ({course}) => {
    const courseCard = course
    console.log(courseCard);
    return (
        <div>
            <h2>Course</h2>
        </div>
    );
};

export default Course;